# Institutional strategy extension point

Implement `DeliveryMetricStrategy`, `ScoringStrategy`, `TrendStrategy`, or `SignalStrategy` from `../DeliveryTypes` here (or in application code) once validated institutional rules are available. The engine deliberately supplies no default trading thresholds, score weights, or institutional interpretations.
